job_spec:
  resources:
    limits:
      memory: 64G
      cpu: 2
      nvidia.com/gpu: 1
    requests:
      memory: 64G
      cpu: 2
      nvidia.com/gpu: 1
  graphics-card: NVIDIA-A10
output_dir: output_dir
model_keyword_args:
  test_arg: "this is a test"